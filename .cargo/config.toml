# GPUI's deeply nested element types can overflow rustc's default worker stack
# while compiling kea-app, especially the test harness. This affects only the
# compiler process, not Kea's runtime stack.
[env]
RUST_MIN_STACK = { value = "16777216", force = false }
