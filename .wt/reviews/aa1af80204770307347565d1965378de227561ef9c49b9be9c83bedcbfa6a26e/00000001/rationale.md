Confirmed concern, leave for a context-owned UI regression test: rendering.rs:376 checks input_context.shell_ready(), which accepts a ready remote/nested POSIX or PowerShell context, while document.directory() holds the last reported directory independently. After a local cwd report, a remote ready marker without a matching directory report can present that stale local cwd as Current shell directory. AGENTS.md permits Current only for a confirmed integrated local-shell prompt; consider local_shell_ready() and explicit tests of remote transition, historical view and directory ownership.
