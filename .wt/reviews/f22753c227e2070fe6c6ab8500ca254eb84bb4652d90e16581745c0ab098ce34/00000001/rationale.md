Acceptable: current document_view.rs:380-392 makes Latest an explicit one-shot navigation intent. The click clears `page_start`, sets `latest_navigation`, and requests a render; render_document takes that intent at line 73, skips the live-output page pin at lines 77-89, then performs the deferred `scroll_to_bottom()` at lines 116-118. This avoids render-before-prepaint geometry pinning and is user-requested navigation, not implicit live-output scrolling. The full regression at tests/unit/app/document_view.rs:75-158 covers wheel-only reading, append preservation, Latest recovery, and resumed following.
