Acceptable: rendering.rs:381 uses `input_context.local_shell_ready()`, not the broader remote-capable shell predicate. That guard requires an explicit Ready marker, id `local`, and POSIX or PowerShell kind (terminal/context.rs:49-53). Normal input invalidates readiness, while nested/remote contexts use distinct ids and fall through to `Shell directory (last reported)`. The displayed current label is therefore restricted to the explicitly reported integrated local shell prompt.
