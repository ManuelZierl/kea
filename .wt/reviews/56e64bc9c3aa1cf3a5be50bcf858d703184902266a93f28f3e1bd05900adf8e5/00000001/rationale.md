Acceptable: current document_view.rs:263-280 computes focus/nonempty-selection state before changing an existing block. A reading view sets `pending` and performs neither `set_value` nor `follow_output_tail`; only the non-reading branch refreshes and follows. The helper at editor/command.rs:313-333 rechecks selection and restores focus, so live output does not replace a reading snapshot.
