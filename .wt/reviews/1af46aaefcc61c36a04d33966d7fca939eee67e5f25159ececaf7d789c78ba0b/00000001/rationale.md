Acceptable: current document_view.rs:238 calls `follow_output_tail` only while creating a previously unseen block editor. The live-output page pin at lines 73-89 records above-tail geometry before new content changes the latest-page offset, even without focus or selection. The helper at editor/command.rs:313-333 refuses to move a nonempty selection and restores prior focus. New-editor tail setup therefore does not erase an existing reading viewport.
