Acceptable: current document_view.rs:231 calls `follow_output_tail` only while creating a previously unseen block editor. The current page-pin guard at lines 74-85 first records a latest-page view as reading whenever the scroll geometry is above the tail, even without focus or selection. The helper at editor/command.rs:313 also refuses to move a nonempty selection and restores prior focus. New-editor tail setup therefore does not erase an existing reading viewport.
