Acceptable: current composer.rs:327-343 is the sole dispatch for both local `completion::suggest` and provider `Endpoint::complete`, and both run inside `std::thread::spawn`. The UI-side workspace pump at workspace.rs:86 uses nonblocking `try_recv` and applies results only after generation, focus, draft, cursor, and IME checks at lines 88-102. Filesystem scans remain bounded and provider I/O is deadline-bounded at provider.rs:111-133; these positives do not block the UI thread.
