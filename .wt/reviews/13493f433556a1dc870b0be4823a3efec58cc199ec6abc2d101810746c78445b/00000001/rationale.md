Acceptable: current document_view.rs:249 scrolls the outer inspector only when `page_start` is unset and `is_at_document_tail` is true. Lines 74-85 set the page pin before output changes when `offset.y + max_offset.height` shows the user above the tail, covering the previously missing no-focus/no-selection case. `tests/unit/app/document_view.rs:28-53` verifies the geometry predicate after a wheel scroll above the tail.
