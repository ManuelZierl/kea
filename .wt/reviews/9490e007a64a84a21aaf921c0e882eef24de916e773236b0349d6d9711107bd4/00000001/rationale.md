Confirmed concern, leave for targeted UI coverage: document_view.rs:220 scrolls the outer block inspector to bottom whenever a previously unseen block gets an editor and page_start is None. The latest page can still be manually scrolled above the tail with an older block selected/focused. There is no check for that reader state, so arriving blocks can move the reading viewport. Distinguish an actually new block from paging/rendering and add a guarded scroll test before changing behavior.
