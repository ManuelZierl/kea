Acceptable: update path runs only in the non-reading branch. document_view.rs computes `reading` from focus or a nonempty selection and sets `pending=true` without touching text when reading. `follow_output_tail` here executes only in the `else` branch, and the helper itself re-checks for a nonempty selection before moving the caret and restores focus. Reading selections are preserved via the pending refresh button.
