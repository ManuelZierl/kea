Acceptable: current document_view.rs:244-257 arms outer `scroll_to_bottom()` only when `page_start` is unset, `is_at_document_tail` is true, and no visible block is focused or selected. Lines 77-89 page-pin a wheel-only reader before output changes the layout. The full KeaView regression at tests/unit/app/document_view.rs:94-133 asserts the above-tail append preserves both page pin and offset, and its mutation-sensitive geometry guard is represented by the current production predicate.
