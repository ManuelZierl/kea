Acceptable: new block editor creation only. document_view.rs creates the editor only when `!visible.contains_key(&id)`, so there is no existing reader selection in this editor to reset. `follow_output_tail` (editor/command.rs:313) checks `selected_text_range` for a nonempty selection before moving the caret and restores prior focus, so tail-follow affects only the new editor. Outer scroll is separately guarded below.
